using System;
using TMPro;
using UnityEngine;

public class MoneyCollector : MonoBehaviour
{
    //Rigidbody2D body;
    //public byte count;
    //public TMP_Text moneyCount_text;

    //GameObject moneyIcon, dynamicCount;

    //void Start()
    //{
    //    body = GetComponent<Rigidbody2D>();
    //    moneyCount_text.text = SaveManager.instance.activeSave.money.ToString();
    //}

    //void OnCollisionEnter2D(Collision2D col)
    //{
    //    if (col.gameObject.CompareTag("Player"))
    //    {
    //        count = Convert.ToByte(UnityEngine.Random.Range(1, 3));
    //        SaveManager.instance.activeSave.money += count;
    //        SaveManager.instance.Save();
    //        moneyCount_text.text = SaveManager.instance.activeSave.money.ToString();
    //        Destroy(gameObject);
    //    }
    //}
}